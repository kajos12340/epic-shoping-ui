import styled from 'styled-components';
import { BottomNavigation as BottomNavigationDefault } from '@material-ui/core';

// eslint-disable-next-line import/prefer-default-export
export const BottomNavigation = styled(BottomNavigationDefault)`
  position: fixed;
  bottom: 0;
  width: 100%;
`;
